1. Setup vcan0 by running setup.sh
2. Run the test bench file (can_network_tb.cpp) trying to mimic the CAN Network 
4. Run comm_temp_test.cpp (compile with the tag "-pthread") (This has to be done berfore running the js file)
3. Run the node server in ../Base/client_c_socket.js
5. Open http://localhost:8081/index.html for displaying the data and sending commands
